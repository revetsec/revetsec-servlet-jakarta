# Claims allowlist for ClaimsLintTests (a copy of Revetsec core's test).
#
# Format: one entry per line, <relative-path>|<exact substring>|<evidence-or-reason>
# Lines starting with a hash are comments. Paths are relative to the repository root.
#
# A banned term in a scanned file passes only when it falls inside the exact substring of an
# entry for that file. Every entry must still match its file, so stale entries fail the build.
# Each entry states the evidence for its wording, or why the wording is not a claim.

README.md|Revetsec has not been independently audited.|Required disclosure; states the absence of an audit and makes no claim.
SECURITY.md|Revetsec has not been independently audited.|Required disclosure (SECURITY.md is Revetsec core's, copied verbatim); states the absence of an audit and makes no claim.
